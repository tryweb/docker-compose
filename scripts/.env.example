# ----------------------------------------------------------------------
# AI Log Processor腳本 (@scripts/ai_proc_log.sh) 設定
# ----------------------------------------------------------------------

# 選擇要使用的 API 服務: "ollama" 或 "openrouter"
API_SERVICE="ollama"

# --- Ollama 設定 ---
# 如果你使用 Ollama，請設定以下參數
OLLAMA_API_URL="http://localhost:11434/api/generate"
OLLAMA_MODEL="llama3"
OLLAMA_MODEL_BAK="deepseek-v3.1"

# --- OpenRouter 設定 ---
# 如果你使用 OpenRouter，請設定你的 API 金鑰和模型
# OPENROUTER_API_KEY="YOUR_OPENROUTER_API_KEY"
# OPENROUTER_MODEL="openai/gpt-4o-mini"
# OPENROUTER_MODEL_BAK="deepseek/deepseek-chat-v3.1"

# ----------------------------------------------------------------------
# Rsync Backup腳本 (@scripts/rsync_backup.sh) 設定
# ----------------------------------------------------------------------

# 定義遠端設定檔
# REMOTE_CONFIG_URL="https://gist.githubusercontent.com/YourUsername/YourGistID/raw/.../log_analyzer.conf"

# --- 基本備份設定 ---
# 備份來源目錄 (此為必須項目，建議在命令列中透過 --source 指定)
# SOURCE_DIR="/path/to/source"

# 備份目標根目錄
DEST_ROOT="/volumeUSB1/usbshare1-2/Data-Server"

# 備份任務名稱
NAME="rsync_backup"

# rsync 頻寬限制 (KB/s)
BW_LIMIT=10000

# rsync 複製完整檔案（預設就是關閉）
# RSYNC_WHOLE_FILE=false

# --- Log 設定 ---
# Log 檔案存放的根目錄
LOG_DIR="/var/log/rsynclog"

# Log 檔案保留天數
RETENTION_DAYS=7

# --- 排除檔案設定 ---
# 全域排除檔案的路徑 (每行一個排除模式)
#EXCLUDE_FILE="/volume1/homes/nonecopy"

# 排除模式 (以空格分隔)
# 例如: RSYNC_EXCLUDE_PATTERNS="#recycle .DS_Store 'some dir'"
#RSYNC_EXCLUDE_PATTERNS="#recycle"


# ----------------------------------------------------------------------
# NFS 掛載點檢查設定
# ----------------------------------------------------------------------

# NFS 掛載點清單，格式為 JSON 陣列
# 腳本會檢查陣列中每個物件的 key（掛載路徑）是否已掛載
#NFS_MOUNTS='[
#  {"/nfs-immich":"192.168.1.2:/pbs-zpool/immich"},
#  {"/nas-246-Photos/ImmichPhotos":"192.168.1.6:/Photos/ImmichPhotos"}
#]'


# ----------------------------------------------------------------------
# Discord 通知與 AI 摘要設定
# ----------------------------------------------------------------------

# --- Discord 通知 ---
# 是否發送 Discord 通知 (true/false)
SEND_DISCORD=true

# send_logs_to_discord.sh 腳本的路徑
DISCORD_SCRIPT="/root/scripts/send_logs_to_discord.sh"

# Discord Webhook URL
DISCORD_WEBHOOK="https://discord.com/api/webhooks/..."

# --- AI 摘要 ---
# 是否啟用 AI 摘要 (true/false)
SUMMARIZE=true

# ai_proc_log.sh 腳本的路徑
AI_SCRIPT="/root/scripts/ai_proc_log.sh"

# 在進行摘要前，要從 Log 中排除的文字模式 (grep -v 的正則表達式)
GREP_EXCLUDE_PATTERN="[0-9]%"